<mah:MetroWindow x:Class="CipherView.SelectedPerson"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CipherView"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="CipherView - Person" Height="595" Width="1129">

    <Grid>
        <mah:MetroAnimatedSingleRowTabControl>
            <TabItem Header="Details">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Content="ID:" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,15,0,0" FontSize="16"/>
                    <TextBox Text="{Binding Id}" IsReadOnly="True" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="9,51,0,0" FontSize="19" Width="496"/>

                    <Label Content="Name:" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="9,84,0,0" FontSize="16"/>
                    <TextBox Text="{Binding name}" IsReadOnly="True" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,115,0,0" FontSize="19" Width="495"/>

                    <Label Content="Email:" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,155,0,0" FontSize="16"/>
                    <TextBox Text="{Binding email}" IsReadOnly="True" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,186,0,0" FontSize="19" Width="495"/>

                    <Label Content="Phone:" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="9,224,0,0" FontSize="16"/>
                    <TextBox Text="{Binding phone}" IsReadOnly="True" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,260,0,0" FontSize="19" Width="495"/>

                    <Label Content="Address:" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,295,0,0" FontSize="16"/>
                    <TextBox Text="{Binding address}" IsReadOnly="True" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,328,0,0" FontSize="19" Width="495"/>

                    <Label Content="IP Address:" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,368,0,0" FontSize="16"/>
                    <TextBox Text="{Binding ipaddress}" IsReadOnly="True" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,399,0,0" FontSize="19" Width="495"/>

                    <Label Content="Description:" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0,0,10,245" FontSize="16"/>
                    <TextBox Text="{Binding description}" IsReadOnly="True" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0,0,10,10" Height="234" Width="554"/>

                    <Label Content="Socials:" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,-3,10,0" FontSize="16"/>
                    <TextBox Text="{Binding socials}" IsReadOnly="True" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,35,10,0" Height="193" Width="554"/>
                </Grid>
            </TabItem>
        </mah:MetroAnimatedSingleRowTabControl>
    </Grid>
</mah:MetroWindow>
